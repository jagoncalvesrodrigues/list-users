import styled from "styled-components";

const StyledCheckActives = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;

`;
const StyledLabel = styled.label`

`;
const StyledCheck = styled.input`
    margin-left: 5px;
`;

export {StyledCheck,StyledCheckActives,StyledLabel}